Basic Calculator using Java Swing to do basic operators. Utilizes order of operations to properly evaluate expressions.

Uses CreateButton class and OrderOfOperations class to simplify code and
		also uses getActionEvent() to identify button being pressed
		Can accept multiple inputs and uses order of operations to calculate currect answer